package com.scts.payment.service;

import java.util.HashMap;
import java.util.Map;

public class PayInfoCache {
	public final static Map<String,Object> map = new HashMap<>();
	
	/*
	 * private final static class SingletonHolder { private static final
	 * PayInfoCache INSTANCE = new PayInfoCache(); } private PayInfoCache() {
	 * 
	 * } public static PayInfoCache getInstance() { return SingletonHolder.INSTANCE;
	 * }
	 */
}
